meta {
  name: update
  type: http
  seq: 2
}

post {
  url: http://127.0.0.1:8000/api/v1/admin/main-categories/create
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:multipart-form {
  main_category_name: Vegetables
}

settings {
  encodeUrl: true
  timeout: 0
}
