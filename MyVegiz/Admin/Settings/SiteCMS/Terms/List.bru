meta {
  name: List
  type: http
  seq: 1
}

get {
  url: {{base_url2}}/admin/site_cms/terms
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
